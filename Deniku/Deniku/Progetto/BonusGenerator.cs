using System;
namespace Progetto
{
    public class BonusGenerator
    {
        private const int BONUS_WIDTH = 20;
        private const int BONUS_HEIGHT = 20;

        private const int baseScore = 100;
        private int[] bounds = new int[2];

        public BonusGenerator(int[] bounds)
        {
            this.bounds = bounds;
        }

        
    }
}
